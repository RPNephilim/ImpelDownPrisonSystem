server:
  port: 1111
  servlet:
    context-path: /imps

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/imps?createDatabaseIfNotExist=true
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  security:
    user:
      name: root
      password: root